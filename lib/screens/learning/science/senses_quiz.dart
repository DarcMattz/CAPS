import 'package:flutter/material.dart';

class SensesQuizScreen extends StatelessWidget {
  const SensesQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Quiz"),
      ),
    );
  }
}
